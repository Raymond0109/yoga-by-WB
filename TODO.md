# TODO / 规划 — Yoga 体式分析器

> 版本基线：**v0.3.0**（2026-07-11，见 `VERSION` 与 `releases/v0.3.0/`）。
> 原则：**先把核心功能端到端打磨稳**，数据库（标准库标定与扩库）留到后续专项回顾再完善。

## 当前状态
- 核心链路已通：输入 → MediaPipe 检测（体式 33 点 + 手部 21 点）→ 12 体式标准库对比 → 2D 骨骼/肌肉叠加 + 矫正建议。
- 肌肉叠加位置已修对（纵向纺锤形 + 基于 world landmark 的前后/内外；斜方肌已不再画到脸上）。
- **待解决的主诉**：肌肉张紧度是启发式（目标 `level × 匹配得分`），且依赖用户手动选对体式——选错体式时张力条错位（用户 07-11 实测 handstand 却选了 tree）。

## ✅ 已完成（核心）
- [x] 体式检测 + 手部检测（MediaPipe Tasks API，受管 venv Python 3.13）
- [x] 12 体式标准库（asanas.json，schema v1）
- [x] 对比/纠正引擎（评分 + ok/warn/off + 纠正文案）
- [x] 2D 骨骼渲染（程序化骨杆/骨骺/脊柱/锁骨）
- [x] 2D 肌肉渲染（医学插画风，纵向纺锤形，冷→暖着色）
- [x] 评分面板稳定（build-once / update-in-place，高度不跳）
- [x] 启动脚本 run.sh + 服务端 /ws、/api/asanas

## 🔴 P0 — 核心收尾（优先，近期）
- [ ] **端到端验证相机 / 视频实时输入**：前端 UI 选择项、WebSocket 帧率、未检测到人体时的友好提示（目前多用手动上传图验证）。
- [ ] **错误与边界处理**：超大图/长视频内存、模型未下载、非人体输入的明确反馈与重试。
- [ ] **一键导出当前分析截图（PNG）**：先做轻量截图，再考虑 PDF 报告。

## 🟡 P1 — 提升核心正确性（直接缓解张力错位主诉）
- [ ] **自动体式识别**：用关节角特征初步判定用户当前体式，默认匹配最近标准体式，避免手动选错导致张力条无意义（这正是 07-11 反馈的根因）。
- [ ] **匹配/评分算法打磨**：采集几组真人样本，统计校准 `live = level × match` 的映射，避免部分体式过严/过松。

## 🟢 P2 — 数据库完善（推迟，后续回顾专项做）
- [ ] **标准库扩库**：补倒立（handstand 类，用户已点名）、手臂平衡、坐立、前屈、后弯等常见体式。
- [ ] **每体式肌肉 `level` 标定**：基于真人教学/解剖资料，给每体式目标肌群打分，替代当前启发默认值。
- [ ] **`rules.target/tol` 真人实测校准**：用采集的真人样本微调默认目标角与容差。
- [ ] **肌肉张力模型升级**：从 `level × match` 启发式，改为结合关节力矩/支撑关系的更可信估算（仍非 EMG）。

## ⚪ 计划书遗留（v2 / 架构级）
- [ ] 3D 解剖 avatar 窗口（Three.js）双视图对照。
- [ ] PDF 报告导出与历史记录。
- [ ] 录制工具：用户自己录标准体式进库。

## 反查方式
- 版本快照：`releases/v0.3.0/`（含 `RELEASE_NOTES.md`，写明恢复步骤）。
- 版本标识：`VERSION`。
- 开发日志：`.workbuddy/memory/` 每日记录；长期约定见 `.workbuddy/memory/MEMORY.md`。
